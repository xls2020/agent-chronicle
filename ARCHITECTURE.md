# Agent Chronicle — 架构文档

> [English](ARCHITECTURE_EN.md) | 中文 · [README](README.md) / [README_EN.md](README_EN.md)

## 项目定位

**本项目的核心价值不在于"AI 写了小说"，而在于"AI 把事实改写为小说时，留下了可追溯的偏移"。**

我们记录工作智能体的真实行为、思考和语言，然后让一个独立的写作智能体（novelist）基于这些事实，按照预设流程生成小说。每一段小说的对应事实关系被完整保留。

```
工作智能体团队 → 行为/思考/语言 → 机械记录（完整日志，私有）
                                        │
                                        ├──（完整日志不公开）
                                        │
                                        ├── 脱敏样本（公开）
                                        │
                                        ▼
                          人类/系统提示（预设流程，不改写事实）
                                        │
                                        ▼
                          novelist 写作智能体（独立）
                                        │
                                        ▼
                              小说章节（公开）
                                        │
                                        ▼
                          对照关系（小说段落 → 脱敏样本条目）
```

**从事实到小说的距离，就是这个项目的全部研究内容。**

---

## 核心概念：偏移

"偏移"指的是：原始记录中的一条事实，在小说中被如何选择、放大、省略、隐喻化或情绪化改写。

```
原始记录（脱敏后）：
命令行子进程成功了！返回 ok，9秒。

小说文本：
九秒。{status: ok}。他盯着这行输出，先是没反应过来，然后长舒一口气。
```

从"9秒返回"到"他盯着输出长舒一口气"，这个距离就是偏移。偏移不是错误，不是幻觉，
而是写作智能体对事实的**文学化处理**。我们不做判断，只保存并展示这个偏移。

### 偏移分类（A–F）

| 类型 | 含义 | 示例 |
|------|------|------|
| **A 忠实** | 事件/引文/数字原样保留 | "9秒返回" → "九秒" |
| **B 比喻** | 事实不变，表达加隐喻 | "9秒返回" → "他长舒一口气" |
| **C 技术压缩** | 多事件压缩为一行技术摘要 | 三次重试 → "他不信邪，重试" |
| **D 氛围句** | 无事件支撑的环境铺陈 | "屋子静得只剩风扇声" |
| **E 动机/主题外推** | 赋予角色内心动机或主题总结 | "是那一瞬间愿意换一条路的清醒" |
| **F 事实性误引** | 新增事件流不存在的事实主张 | 需在对照中标注 |
| **G 跨章外推** | 用角色模板补写当天缺失过程 | 组装师场景 |

---

## 三层结构

### 第一层：机械记录层（Recorder）

- **职责**：捕获工作智能体的行为、思考、语言和人类提示词
- **原则**：**不加工、不判断、不贴标签**
- **输出**：结构化事件记录（完整日志私有）
- **模块**：`src/recorder/`（event_capture / storage / desensitize）

### 第二层：叙事生成层（Narrator）

- **职责**：novelist 写作智能体基于机械记录，按照预设流程生成小说
- **原则**：**基于真实事实，不凭空创造事件**
- **输出**：小说章节（公开）
- **模块**：`src/narrator/`（llm_adapter / storyteller / adapters）

### 第三层：对照层（Mapping）

- **职责**：建立小说段落与原始记录条目之间的对应关系
- **原则**：**可追溯、可验证、可研究**
- **输出**：对照表（公开）
- **文件**：`samples/mapping.json`

---

## 数据流

```
多智能体系统（工作智能体）
        │
        │ 行为/思考/语言 + 人类提示词
        ▼
机械记录层（Recorder）
        │
        ├── 完整日志（私有，不公开）
        │
        ├── 脱敏处理（desensitize.py）
        │       │
        │       ▼
        │   脱敏样本（公开）
        │
        ▼
写作智能体（novelist subagent）
        │
        │ 预设流程：基于真实记录写小说
        ▼
小说章节（公开）
        │
        ▼
对照层（mapping.json）
        │
        ├── 小说段落 → 脱敏样本条目
        │
        └── 展示"偏移"
```

---

## 公开边界

| 内容 | 状态 | 原因 |
|------|------|------|
| 小说全文 | 公开 | 项目门面，文学性改写 |
| 脱敏日志样本 | 公开 | 展示事实来源，保护敏感信息 |
| 对照关系 | 公开 | 核心研究接口 |
| 完整日志 | 私有 | 包含正在开发中的系统细节 |
| 机械记录代码 | 公开 | 方法可复现 |
| 叙事生成代码 | 公开 | 方法可复现 |
| 核心系统代码 | 私有 | 护城河 |

---

## 脱敏规则

脱敏样本保留：
- 事件类型（action / thought / utterance / human_prompt）
- 时间序列关系
- 基本技术事实（如"子进程挂死""参数错误""timeout"）
- 与小说段落对应的必要细节

脱敏样本替换或隐藏：
- 项目名称 → `project_x`
- 文件路径 → `/path/to/`
- API 密钥和 token → `***`
- 具体的技术栈名称（内部模块名）→ 通用描述
- 任何可追溯到未发布功能的信息

规则文件：`samples/desensitization_rules.json`（发布前请扩展 `blacklist` 为你自己的私有词表）

---

## 研究接口

研究者使用本项目，可以：

1. 阅读小说全文，获得第一印象
2. 查看脱敏样本，理解事实来源
3. 分析对照关系，定位"偏移"
4. 在自己的系统中复现这个过程

**本项目提供的是方法，不是完整数据集。完整数据集属于私有系统。**
